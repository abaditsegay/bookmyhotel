package com.bookmyhotel.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web MVC configuration for the BookMyHotel application.
 * 
 * Note: CORS configuration is handled in SecurityConfig.java for better
 * integration with Spring Security and to avoid conflicts.
 */
@Configuration
public class WebConfig {
    // CORS configuration is centralized in SecurityConfig.java
}
